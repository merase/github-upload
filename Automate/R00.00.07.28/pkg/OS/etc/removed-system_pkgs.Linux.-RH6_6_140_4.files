#
# A list with Packages which need to be added if passing by this kernel.
# - It is a list with a single file name on each line.
# - Comments (#) may be used at the start of the line only or behind a file
# - Empty lines are ignore
# - The file names should NOT include the version, OS release or Architecture
#

hpacucli
