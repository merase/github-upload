#!/bin/sh
#
# This funciton set the generic command variables which are valid for Linux
# 

readonly CMD_install='pkgadd -d'            # install : Installs an file (in this case an pkg). Expect a file name
readonly CMD_install_ext='pkg'              # used to identify package extension
